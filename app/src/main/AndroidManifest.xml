<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dristi.kharcha">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Planned_payment"></activity>
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".About" /> <!-- <activity android:name=".TabloginActivity"> -->
        <!-- </activity> -->
        <activity
            android:name=".Navigation_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Lend_Borrow"
            android:label="Lend and Borrow" />
        <activity android:name=".Add_Lend_Borrow" />
        <activity
            android:name=".Budget"
            android:label="Budget" />

        <receiver android:name=".Alarmreceiver"></receiver>
    </application>

</manifest>